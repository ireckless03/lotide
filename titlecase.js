// title case, every new word turn the first letter in to a capital

// given a string
const titleCase = function (string) {
  // loop thru string
  for (character of string) {
    console.log(character)
    console.log(string.character)
  }
// check if first letter is lower case 
// if it's upper case
// check if next letter is upper
// if so change into a lowercase
// continue until there is a space present
// if index contains a space the next character is turned into a capital
//
}
// loop thru string
// check if first letter is lower case 
// if it's upper case
// check if next letter is upper
// if so change into a lowercase
// continue until there is a space present
// if index contains a space the next character is turned into a capital
//
const string = "example 1"